def salary(x):
    x = float(x)
    return (x*13+x*12*0.2)

package = input('package is')
floatpackage = float(package)
floatpackage == salary(x)
print(x)

def total(x):
    x = float(x)
    return (x*13 + x*12*0.2)

total = input('total package:')
floattotal = float(total)
print(floattotal)

def monthly(x):
    floattotal == float(total(x))
    return(x)
monthly(x)